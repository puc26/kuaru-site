---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<h1
  class=`text-heading-1 font-semibold text-4xl sm:text-4xl md:text-5xl font-zen-kaku ${className}`
>
  <slot />
</h1>
